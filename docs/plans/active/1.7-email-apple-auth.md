# Plan 1.7: Email + Apple Sign-In

**Status:** Planned
**Depends on:** 0.4 (Better Auth already configured with Google OAuth)
**Blocks:** Nothing — can be done in parallel with other Phase 1 work

## Goal

Add email/password sign-up and Apple OAuth as additional authentication methods alongside Google OAuth. Users should be able to create an account with email+password, verify their email, reset their password, and sign in with Apple. The login page becomes a multi-method auth page.

## Manual Steps (Before Code)

- `npm install @better-auth/email-otp` (if using OTP-based verification — evaluate vs magic link)
- Choose and install an email sending library (e.g., `resend` or `nodemailer`)
- **Apple Developer setup:**
  - Register an App ID with "Sign in with Apple" capability
  - Create a Services ID for web authentication
  - Generate a private key for Sign in with Apple
  - Configure the return URL: `{BETTER_AUTH_URL}/api/auth/callback/apple`
- **Email provider setup:**
  - Create account with email provider (e.g., Resend)
  - Verify sending domain
  - Get API key
- Add environment variables to `.env`:
  - `APPLE_CLIENT_ID` — Services ID
  - `APPLE_CLIENT_SECRET` — JWT generated from Apple private key (or key components for runtime generation)
  - `EMAIL_FROM` — sender address (e.g., `noreply@telltale.app`)
  - Email provider credentials (e.g., `RESEND_API_KEY`)

## Key Steps

### Email Provider Setup

- Create `src/lib/email.ts`:
  - `import "server-only"` guard
  - Exports a `sendEmail` function: `(to: string, subject: string, html: string) => Promise<void>`
  - Implements with chosen email provider (Resend recommended for simplicity)
  - Reads provider API key from environment
  - This is the only file that knows which email provider is active — same isolation principle as the LLM provider

### Better Auth Configuration

- Update `src/server/auth.ts`:
  - Enable email/password authentication:
    ```typescript
    emailAndPassword: {
      enabled: true,
      requireEmailVerification: true,
      sendResetPassword: async ({ user, url }) => {
        await sendEmail(user.email, "Reset your password", `<a href="${url}">Reset password</a>`);
      },
    },
    ```
  - Add email verification handler:
    ```typescript
    emailVerification: {
      sendOnSignUp: true,
      sendVerificationEmail: async ({ user, url }) => {
        await sendEmail(user.email, "Verify your email", `<a href="${url}">Verify email</a>`);
      },
    },
    ```
  - Add Apple to `socialProviders`:
    ```typescript
    apple: {
      clientId: process.env.APPLE_CLIENT_ID,
      clientSecret: process.env.APPLE_CLIENT_SECRET,
    },
    ```
  - Add environment variable checks for Apple credentials
  - Keep Google provider as-is

### Login Page Updates

- Update `src/app/(auth)/login/page.tsx`:
  - Add email/password sign-in form (email input, password input, submit button)
  - Add "Forgot password?" link below the form
  - Add visual divider ("or continue with") between form and social buttons
  - Add Apple sign-in button alongside existing Google button
  - Add "Don't have an account? Sign up" link
- Create `src/app/(auth)/login/email-sign-in-form.tsx`:
  - Client Component
  - Email + password inputs with submit
  - Calls `authClient.signIn.email({ email, password, callbackURL: "/dashboard" })`
  - Shows inline error for invalid credentials
  - Loading state on submit button
- Create `src/app/(auth)/login/apple-sign-in-button.tsx`:
  - Client Component, same pattern as `GoogleSignInButton`
  - Calls `authClient.signIn.social({ provider: "apple", callbackURL: "/dashboard" })`

### Sign-Up Page

- Create `src/app/(auth)/signup/page.tsx`:
  - Sign-up form: name, email, password, confirm password
  - "Already have an account? Sign in" link
  - On success, redirect to a "check your email" page
- Create `src/app/(auth)/signup/sign-up-form.tsx`:
  - Client Component
  - Calls `authClient.signUp.email({ name, email, password, callbackURL: "/dashboard" })`
  - Client-side validation: password length, passwords match
  - Shows inline errors
  - Loading state on submit
- Create `src/app/(auth)/signup/check-email/page.tsx`:
  - Simple page: "We've sent a verification email to {email}. Please check your inbox."
  - Link back to sign in

### Forgot Password Flow

- Create `src/app/(auth)/forgot-password/page.tsx`:
  - Email input form: "Enter your email and we'll send you a reset link"
  - On submit, calls `authClient.forgetPassword({ email, redirectTo: "/reset-password" })`
  - Shows success message: "If an account exists with that email, we've sent a reset link"
  - Link back to sign in
- Create `src/app/(auth)/reset-password/page.tsx`:
  - Reads token from URL search params
  - New password + confirm password form
  - Calls `authClient.resetPassword({ newPassword, token })`
  - On success, redirect to login with success message

### Middleware Updates

- Update `src/middleware.ts`:
  - Add `/signup`, `/forgot-password`, `/reset-password` to `publicRoutes`

### CSS

- CSS Modules for all new components — follow the existing login page styling patterns
- `src/app/(auth)/login/email-sign-in-form.module.css`
- `src/app/(auth)/login/apple-sign-in-button.module.css`
- `src/app/(auth)/signup/sign-up-form.module.css`
- Update `src/app/(auth)/login/page.module.css` for the multi-method layout (divider, spacing)

### Tests

- Update `tests/server/auth.test.ts`:
  - Test: auth config includes emailAndPassword enabled
  - Test: auth config includes Apple social provider
  - Test: auth config includes email verification handlers
- Write `tests/app/login/email-sign-in-form.test.tsx`:
  - Mock `@/lib/auth-client`
  - Test: renders email and password inputs
  - Test: calls `authClient.signIn.email` on submit
  - Test: shows error on failed sign-in
  - Test: disables button while loading
- Write `tests/app/signup/sign-up-form.test.tsx`:
  - Mock `@/lib/auth-client`
  - Test: renders name, email, password, confirm password inputs
  - Test: calls `authClient.signUp.email` on submit
  - Test: validates passwords match before submit
  - Test: shows error on failed sign-up

## Files Created/Modified

- `src/lib/email.ts` — new (email sending)
- `src/server/auth.ts` — modified (add email/password + Apple provider)
- `src/middleware.ts` — modified (add public routes)
- `src/app/(auth)/login/page.tsx` — modified (multi-method layout)
- `src/app/(auth)/login/page.module.css` — modified (divider, spacing)
- `src/app/(auth)/login/email-sign-in-form.tsx` — new
- `src/app/(auth)/login/email-sign-in-form.module.css` — new
- `src/app/(auth)/login/apple-sign-in-button.tsx` — new
- `src/app/(auth)/login/apple-sign-in-button.module.css` — new
- `src/app/(auth)/signup/page.tsx` — new
- `src/app/(auth)/signup/sign-up-form.tsx` — new
- `src/app/(auth)/signup/sign-up-form.module.css` — new
- `src/app/(auth)/signup/check-email/page.tsx` — new
- `src/app/(auth)/forgot-password/page.tsx` — new
- `src/app/(auth)/reset-password/page.tsx` — new
- `.env.example` — modified (add Apple + email env vars)
- `tests/server/auth.test.ts` — modified
- `tests/app/login/email-sign-in-form.test.tsx` — new
- `tests/app/signup/sign-up-form.test.tsx` — new

## Notes

- **No schema migration needed.** The existing Better Auth schema already has `Account.password` (nullable), `User.emailVerified`, and the `Verification` model. Better Auth generated these fields in Plan 0.4 — they just weren't used with Google-only OAuth.
- **Email verification is required** for email sign-ups. This prevents fake accounts and ensures we can reach the user. Social sign-ins (Google, Apple) skip verification since the provider already verified the email.
- **Email provider choice:** Resend is recommended for simplicity (single API call, good DX). The `src/lib/email.ts` wrapper keeps the provider swappable — same pattern as the LLM provider in 1.2.
- **Apple Sign-In quirks:** Apple only sends the user's name on the first sign-in. Better Auth handles this, but it means if the first sign-in fails after Apple's redirect, the name is lost. Apple also requires HTTPS for the callback URL, so Apple sign-in won't work in local development without a tunnel (e.g., ngrok). Google works fine on localhost.
- **Password security** is handled by Better Auth — it hashes passwords with bcrypt/argon2 internally. No custom hashing needed.
- **"Forgot password" uses generic success message** ("If an account exists...") to prevent email enumeration attacks.
- **Email templates are plain HTML for MVP.** Styled email templates can be added later — the `sendEmail` function signature supports it.
- This plan is independent of the Phase 1 interview flow (1.1–1.6) and can be implemented in parallel.

## Done When

- Sign up with email + password → receive verification email → click link → account verified
- Sign in with verified email + password → redirected to dashboard
- Sign in with unverified email → shown appropriate error
- "Forgot password" → receive reset email → click link → set new password → sign in works
- Sign in with Apple → redirected to dashboard (requires HTTPS / production-like env)
- Login page shows all three methods: email form, Google button, Apple button
- Sign-up page works with validation (password length, passwords match)
- Unauthenticated pages (signup, forgot-password, reset-password) are accessible without redirect
- All tests pass via `npm test`
- `npm run build` succeeds
- `npm run lint` passes
