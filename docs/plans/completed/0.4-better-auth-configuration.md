# Plan 0.4: Better Auth Configuration

**Status:** Complete
**Depends on:** 0.3 (need Prisma schema and working database)
**Blocks:** 0.5, 0.6

## Goal

Better Auth fully configured with Prisma adapter, Google OAuth, sign-in/sign-out pages, middleware protecting the `(app)` route group, and session access server-side via `auth.api.getSession()`.

## Manual Steps (Before Code)

1. **Create Google OAuth credentials:**
   - Go to https://console.cloud.google.com
   - Create project "Telltale" (or use existing)
   - APIs & Services → Credentials → Create OAuth client ID
   - Type: Web application
   - Authorized JS origins: `http://localhost:3000`
   - Authorized redirect URIs: `http://localhost:3000/api/auth/callback/google`
   - Copy Client ID and Client Secret

2. **Generate BETTER_AUTH_SECRET:**
   - Run `openssl rand -base64 33`

3. **Update `.env`:**
   - `BETTER_AUTH_SECRET="<generated>"`
   - `BETTER_AUTH_URL="http://localhost:3000"`
   - `GOOGLE_CLIENT_ID="<client-id>"`
   - `GOOGLE_CLIENT_SECRET="<client-secret>"`

## Key Steps

- Install `better-auth` (pin to `1.4.x`)
- Generate Better Auth schema: `npx @better-auth/cli generate` — outputs Prisma models. **Replace** the existing Auth.js-style auth models (User, Account, Session, VerificationToken) with the generated Better Auth models. The existing models use Auth.js field conventions (e.g., `providerAccountId`, `sessionToken`, `expires`) which are incompatible with Better Auth's expected fields (e.g., `accountId`, `token`, `expiresAt`).
- Reset DB and run a fresh migration (`npx prisma migrate reset` then `npx prisma migrate dev --name better-auth`)
- Create `src/server/auth.ts`:
  - `betterAuth()` with `prismaAdapter(prisma, { provider: "postgresql" })`
  - `socialProviders: { google: { clientId, clientSecret } }`
  - Export `auth` instance
- Create `src/app/api/auth/[...all]/route.ts` — `toNextJsHandler(auth)` exporting `GET`/`POST`
- Create `src/lib/auth-client.ts` — `createAuthClient()` from `better-auth/react`
- Create `src/middleware.ts`:
  - Use `getSessionCookie(request)` from `better-auth/cookies` — cookie-only check, no HTTP round-trip, fast. Full session validation happens in the `(app)` layout via `auth.api.getSession()`.
  - Allow public routes: `/`, `/api/auth/*`
  - Redirect unauthenticated users to `/login`
  - Redirect authenticated users away from `/login` to `/dashboard`
  - Export `config.matcher` excluding `_next/static`, `_next/image`, `favicon.ico`
- Create `src/app/(auth)/layout.tsx` — minimal auth layout
- Create `src/app/(auth)/login/page.tsx` — Server Component shell, reads `?error` query param and shows user-friendly message if present
- Create `src/app/(auth)/login/google-sign-in-button.tsx` — Client Component using `authClient.signIn.social({ provider: "google", callbackURL: "/dashboard" })`
- Create `src/app/(app)/layout.tsx` — calls `auth.api.getSession({ headers: await headers() })` server-side, renders header with user name + sign-out button
- Create `src/app/(app)/sign-out-button.tsx` — Client Component: `await authClient.signOut()` then `router.push("/login")`
- Create `src/app/(app)/dashboard/page.tsx` — placeholder showing welcome message
- Root layout (`src/app/layout.tsx`) — no SessionProvider needed (Better Auth doesn't require one)

## Files Created/Modified

- `src/server/auth.ts` — Better Auth config (replaces stub)
- `src/lib/auth-client.ts` — Better Auth client (NEW)
- `src/app/api/auth/[...all]/route.ts` — Better Auth API handler
- `src/middleware.ts` — route protection
- `src/app/(auth)/layout.tsx` — auth layout
- `src/app/(auth)/login/page.tsx` — sign-in page (Server Component)
- `src/app/(auth)/login/google-sign-in-button.tsx` — Google sign-in button (Client Component)
- `src/app/(app)/layout.tsx` — authenticated app layout (uses `auth.api.getSession()` server-side)
- `src/app/(app)/sign-out-button.tsx` — sign-out button (Client Component)
- `src/app/(app)/dashboard/page.tsx` — dashboard placeholder
- `prisma/schema.prisma` — replaced with Better Auth models (not merged)
- `tests/server/auth.test.ts` — auth config tests
- `tests/middleware.test.ts` — middleware redirect logic tests
- `tests/app/api/auth/route.test.ts` — API route handler tests
- `e2e/auth.spec.ts` — auth E2E smoke tests

## Testing

Backend tests live in `tests/` mirroring `src/` structure (per ADR 010).

**Unit tests (`tests/server/auth.test.ts`):**
- Auth config exports `auth` instance
- Auth instance has expected API methods

**Unit tests (`tests/app/api/auth/route.test.ts`):**
- API route handler exports `GET` and `POST` functions

**Unit tests (`tests/middleware.test.ts`):**
- Unauthenticated request to `/dashboard` redirects to `/login`
- Unauthenticated request to `/` is allowed through
- Unauthenticated request to `/api/auth/callback/google` is allowed through
- Authenticated request to `/login` redirects to `/dashboard`
- Authenticated request to `/dashboard` is allowed through

**E2E tests (`e2e/auth.spec.ts`):**
- Full OAuth flow is deferred — requires real Google credentials and is better suited for manual verification or CI with test credentials
- E2E smoke test: visiting `/dashboard` without auth redirects to `/login`
- E2E smoke test: `/login` page renders with Google sign-in button

## Notes

- Email provider omitted for Phase 0 — Google OAuth is sufficient to prove auth works
- Better Auth uses `BETTER_AUTH_` env var prefix for its own config; Google credentials use standard `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`
- Better Auth uses DB sessions with signed cookie caching — no JWT strategy needed
- No type augmentation needed — Better Auth uses `$Infer` for type inference
- No SessionProvider needed — `authClient` from `better-auth/react` works without a provider
- Sign-in and sign-out buttons are Client Components (use `authClient.signIn.social()` and `authClient.signOut()`)
- Pin `better-auth` to `1.4.x` — pre-2.0 API could shift between minor versions
- The `(app)` layout's `auth.api.getSession()` check only runs on initial/hard page loads, not client-side navigations between `(app)` routes (Next.js App Router layouts persist across navigations). Middleware handles route protection for client-side transitions via the cookie check.
- The existing Prisma schema uses Auth.js field conventions — these must be fully replaced, not merged, when generating Better Auth models

## Done When

- Browsing to `/dashboard` while not signed in redirects to `/login`
- Clicking "Sign in with Google" goes through OAuth flow
- After auth, user lands on `/dashboard` and sees their name
- User and Account tables populated in Neon (verify via `npx prisma studio`)
- Clicking "Sign out" ends session and redirects to `/login`
- Home page (`/`) accessible without authentication
- All unit tests pass (`npm test`)
- E2E auth smoke tests pass (`npm run test:e2e`)
