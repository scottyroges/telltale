Map codebase architecture for a feature, area, or general onboarding.

Usage:
  /explore                         — general codebase overview
  /explore auth                    — explain how authentication works
  /explore "add email notifications" — map what's relevant for a task

Dispatch this to the onboarder agent. This is for understanding architecture and patterns, not for finding specific files (use the built-in Explore for that).
