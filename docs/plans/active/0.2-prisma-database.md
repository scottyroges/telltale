# Plan 0.2: Prisma + Database

**Status:** Not Started
**Depends on:** 0.1 (need `package.json` and project structure)
**Blocks:** 0.3, 0.4, 0.5

## Goal

Prisma ORM with Auth.js models (User, Account, Session, VerificationToken), Neon PostgreSQL connection, initial migration, client singleton, and seed script. Schema is minimal — no Book/Story/etc yet.

## Manual Steps (Before Code)

1. **Create Neon account** at https://neon.tech (free tier)
2. **Create project** named "telltale" — choose region closest to your users
3. **Copy connection strings:**
   - Pooled connection (has `-pooler` in hostname) → `DATABASE_URL`
   - Direct connection (no `-pooler`) → `DIRECT_URL`
4. **Create `.env`** from `.env.example`, fill in both connection strings

## Key Steps

- Install `prisma`, `@prisma/client`, `@prisma/adapter-neon`, `@neondatabase/serverless`, `tsx`
- `npx prisma init --datasource-provider postgresql`
- Write `prisma/schema.prisma`:
  - Generator with `previewFeatures = ["driverAdapters"]` (check if GA in current Prisma version)
  - Datasource with `url` (pooled) and `directUrl` (direct)
  - User, Account, Session, VerificationToken models per Auth.js Prisma adapter spec
  - `createdAt`/`updatedAt` on User, Account, Session
- Create `src/lib/prisma.ts` — singleton using Neon serverless driver + `globalThis` caching for dev
- Create `prisma/seed.ts` — upserts a dev user (`dev@telltale.local`)
- Add to `package.json`:
  - `"postinstall": "prisma generate"`
  - `"db:migrate": "prisma migrate dev"`
  - `"db:push": "prisma db push"`
  - `"db:seed": "prisma db seed"`
  - `"db:studio": "prisma studio"`
  - `"prisma": { "seed": "tsx prisma/seed.ts" }`
- Run `npx prisma migrate dev --name init`
- Update `.env.example` to include `DIRECT_URL`

## Files Created/Modified

- `prisma/schema.prisma` — Prisma schema
- `prisma/seed.ts` — seed script
- `prisma/migrations/<timestamp>_init/migration.sql` — initial migration
- `src/lib/prisma.ts` — Prisma client singleton
- `.env.example` — updated with `DIRECT_URL`
- `package.json` — new scripts + prisma seed config

## Done When

- `npx prisma migrate dev` runs successfully, creates tables in Neon
- `npx prisma db seed` creates the test user
- `npx prisma studio` shows User, Account, Session, VerificationToken tables with seeded user
- Importing `prisma` from `@/lib/prisma` works in server-side code
- Migration file exists at `prisma/migrations/`
